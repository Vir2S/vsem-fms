# VSEM FMS package

This directory contains the application package and Docker assets for VSEM FMS.

For installation, API usage, Docker commands, configuration, and tests, see the repository-level [`README.md`](../README.md).

Run the application from the repository root:

```bash
python -m vsem_fms.app.main
```
